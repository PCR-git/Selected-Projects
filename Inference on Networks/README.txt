
To run this code:

Upload the Python notebook to a Jupyter Notebook.
Upload the file facebook_combined.txt, which contains the Facebook data used in the paper.
Run the cells in order.

In the final section, on inference in Problog, the Python code outputs Problog code,
which then must be run in Problog.